# Spotify API credentials
# Get these from: https://developer.spotify.com/dashboard/applications
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI="http://localhost:8888/callback"

# Optional: Set the logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"

# Optional: Custom output directory (defaults to "Music")
SPOTIFYSAVER_OUTPUT_DIR=Music

# Optional: YouTube cookies file path for age-restricted content
YTDLP_COOKIES_PATH="cookies.txt"

# Optional: API Configuration
# API_PORT: Port for the API server (default is 8000)
API_PORT=8000
# API_HOST: Host for the API server (default is "0.0.0.0")
API_HOST="0.0.0.0"

# Optional: UI Language setting (en for English, es for Spanish)
# Default is "en" for English
UI_LANGUAGE=en